(()=>{const lines=[...document.querySelectorAll('[data-type]')];if(window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;let index=0;for(const line of lines){const text=line.textContent;line.setAttribute('aria-hidden','true');line.replaceChildren();for(const char of text){const span=document.createElement('span');span.className='typed-char';span.textContent=char===' '? '\u00a0':char;span.style.animationDelay=(index++*48)+'ms';line.append(span)}}})();